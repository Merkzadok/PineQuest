{
  "name": "pinequest",
  "private": true,
  "scripts": {
    "dev:front": "bun --cwd client dev",
    "dev:back": "bun --cwd server dev",
    "dev": "concurrently \"bun run dev:frontend\" \"bun run dev:backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "ts-node-dev": "^2.0.0"
  },
  "dependencies": {
    "@apollo/server": "^5.0.0",
    "express": "^5.1.0",
    "graphql-tag": "^2.12.6"
  }
}
